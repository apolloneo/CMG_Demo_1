<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HomeScreenActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/homeScreenBackground</item>
    </style>

    <style name="SegmentDisplay">
        <item name="android:textColor">@color/colorRed</item>
        <item name="android:fontFamily">@font/dseg7</item>
        <item name="android:padding">4dp</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:gravity">end|center_vertical</item>
        <item name="android:background">@drawable/independent_display</item>
        <item name="android:singleLine">true</item>
        <item name="android:maxLines">1</item>
        <item name="android:maxLength">3</item>
    </style>

    <style name="MsgDisplay">
        <item name="android:textColor">@color/colorRed</item>
        <item name="android:fontFamily">@font/dm5_7</item>
        <item name="android:padding">4dp</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/independent_display</item>
        <item name="android:singleLine">true</item>
        <item name="android:maxLines">1</item>
        <item name="android:maxLength">5</item>
    </style>

    <style name="Divider">
        <item name="android:background">@android:color/black</item>
    </style>

    <style name="Divider.Horizontal" parent="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>

    </style>

    <style name="Divider.Vertical" parent="Divider">
        <item name="android:layout_width">2dp</item>
        <item name="android:layout_height">0dp</item>
    </style>

</resources>
